{
  "firestore": {
    "rules": "firestore.rules"
  },
  "functions": [
    {
      "source": "stripe_webhook",
      "codebase": "stripe_webhook",
      "ignore": [
        "node_modules",
        ".git",
        "firebase-debug.log",
        "firebase-debug.*.log",
        "*.local"
      ],
      "predeploy": [
        "npm --prefix \"$RESOURCE_DIR\" run lint"
      ]
    }
  ]
}